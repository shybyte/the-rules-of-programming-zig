{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tests in current file",
            "type": "shell",
            "command": "zig",
            "args": [
                "test",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Runs the Zig tests defined in the current file",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}